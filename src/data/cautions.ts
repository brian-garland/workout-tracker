import { EXERCISES as E } from './exerciseNames'

// Exercises and movements that require extra caution
export const cautionExercises: Record<string, string> = {
  [E.BULGARIAN_SPLIT_SQUAT]: 'High patellar load — earn the depth. Stop well above 90 degrees initially.',
  [E.BULGARIAN_SPLIT_SQUAT_FULL]: 'High patellar load — only progress to full depth if pain-free.',
}
